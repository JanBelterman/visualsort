package Config;

public class Config {

    public static int FRAME_WIDTH = 1200;
    public static int FRAME_HEIGHT = 600;

    public static int SLEEP_MILLIS = 10; // TODO: dynamic for each because some take longer to paint

    public static int ARRAY_SIZE = 200; // TODO: dynamic for each sorting algorithm, because some take longer to paint

}
